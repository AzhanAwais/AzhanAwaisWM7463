// var stack=[];

function getChar(num)
{
    alert(num);
    // if(char==="+" || char==="-" || char==="*" || char==="%" || char==="sqrt" || char==="sq" )
    // {
    //     var fpop=stack.pop();
    //     alert(fpop);
    // }
    // else
    // { 
    //     alert(stack.push(char));
    // }
}

